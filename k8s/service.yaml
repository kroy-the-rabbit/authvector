apiVersion: v1
kind: Service
metadata:
  name: authvector
  namespace: authvector
spec:
  selector:
    app: authvector
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
